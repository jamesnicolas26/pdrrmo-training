# <IfModule mod_rewrite.c>
#   RewriteEngine On
#   RewriteBase /pdrrmo-training/

#   # ✅ Let real files and folders through
#   RewriteCond %{REQUEST_FILENAME} -f [OR]
#   RewriteCond %{REQUEST_FILENAME} -d
#   RewriteRule ^ - [L]

#   # ✅ Let React handle all routes, including /reset-password/*
#   RewriteRule ^ index.html [L]
# </IfModule>
<IfModule mod_rewrite.c>
  RewriteEngine On
  RewriteBase /pdrrmo/wp-content/reactpress/apps/pdrrmo-training/

  # Allow direct access to real files
  RewriteCond %{REQUEST_FILENAME} -f [OR]
  RewriteCond %{REQUEST_FILENAME} -d
  RewriteRule ^ - [L]

  # Redirect all other requests to index.html
  RewriteRule ^ index.html [L]
</IfModule>
